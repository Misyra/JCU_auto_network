# 校园网认证配置模板
# 复制此文件为 .env 并填入您的实际配置信息

# ==================== 校园网认证信息 ====================
# 校园网用户名（学号+运营商后缀，如：202012345@cmcc）
# 运营商后缀：@cmcc(移动) @unicom(联通) @telecom(电信) @xyw(校园网)
CAMPUS_USERNAME=your_username_here

# 校园网登录密码
CAMPUS_PASSWORD=your_password_here

# 校园网认证页面地址（通常为网关地址）
CAMPUS_AUTH_URL=http://172.29.0.2

# ==================== 浏览器配置 ====================
# 是否启用无头模式（true=后台运行不显示浏览器窗口，false=显示浏览器窗口）
# 推荐设置为 true，特别是在服务器环境中
HEADLESS=true

# 浏览器操作超时时间（秒）
# 如果网络较慢可适当增加此值
BROWSER_TIMEOUT=30

# 页面加载超时时间（秒）
# 认证页面加载的最大等待时间
PAGE_LOAD_TIMEOUT=15

# ==================== 网络检测配置 ====================
# 网络状态检测间隔（秒）
# 建议设置为 240-600 秒，避免过于频繁的检测
CHECK_INTERVAL=60

# 登录失败时的最大重试次数
# 超过此次数后将进入冷却期
MAX_RETRY_ATTEMPTS=3

# 重试间隔时间（秒）
# 每次重试之间的等待时间
RETRY_DELAY=5

# 是否自动启动监控模式（仅GUI版本有效）
# true=启动后自动开始监控，false=手动启动监控
AUTO_START_MONITORING=false

# ==================== 日志配置 ====================
# 日志级别（DEBUG/INFO/WARNING/ERROR）
# DEBUG=详细调试信息，INFO=一般信息，WARNING=警告，ERROR=仅错误
LOG_LEVEL=INFO

# 是否将日志保存到文件
# true=同时输出到控制台和文件，false=仅输出到控制台
LOG_TO_FILE=true

# 日志文件保存路径
# 相对路径基于程序运行目录，绝对路径直接指定位置
LOG_FILE_PATH=logs/network_monitor.log

# 安全提醒：
# 1. 请勿将包含真实密码的 .env 文件提交到版本控制系统
# 2. 确保 .env 文件的访问权限仅限于当前用户
# 3. 定期更换密码以确保账户安全