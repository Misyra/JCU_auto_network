# 校园网自动认证工具配置模板
# 复制此文件为 .env 并填写实际值

# ============= 基础认证配置 =============
# 校园网用户名（必填）
CAMPUS_USERNAME=your_username_here

# 校园网密码（必填）
CAMPUS_PASSWORD=your_password_here

# 校园网认证地址（必填）
CAMPUS_AUTH_URL=http://172.29.0.2

# 运营商后缀（可选）
# @cmcc(移动) @unicom(联通) @telecom(电信) @xyw(教育网) 或留空
CAMPUS_ISP=@cmcc

# ============= 浏览器配置 =============
# 无头模式运行（true=后台运行，false=显示浏览器窗口）
BROWSER_HEADLESS=false

# 页面加载超时时间（毫秒，从10000优化为8000）
BROWSER_TIMEOUT=8000

# 低资源模式（启用后将禁用图片、插件等以减少内存占用）
BROWSER_LOW_RESOURCE_MODE=true

# 自定义User-Agent（可选，留空使用默认固定值）
BROWSER_USER_AGENT=

# ============= 网络监控配置 =============
# 网络检测间隔（秒，默认240秒=4分钟）
MONITOR_INTERVAL=240

# 自动启动监控（GUI启动时是否自动开始监控）
AUTO_START_MONITORING=false

# Ping测试目标（逗号分隔）
PING_TARGETS=8.8.8.8,114.114.114.114,baidu.com

# ============= 重试策略配置 =============
# 最大重试次数
RETRY_MAX_RETRIES=3

# 重试间隔（秒）
RETRY_INTERVAL=5

# ============= 暂停登录时段配置 =============
# 是否启用暂停登录时段（避免深夜频繁认证）
PAUSE_LOGIN_ENABLED=true

# 暂停登录开始时间（24小时制，0-23）
PAUSE_LOGIN_START_HOUR=0

# 暂停登录结束时间（24小时制，0-23）
PAUSE_LOGIN_END_HOUR=6

# ============= 日志配置 =============
# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# 日志格式
LOG_FORMAT=%(asctime)s - %(levelname)s - %(message)s

# 日志文件路径（留空则不写入文件）
LOG_FILE=logs/campus_auth.log

# ============= 安全提醒 =============
# 1. 请确保 .env 文件权限设置为 600（仅所有者可读写）
# 2. 不要将包含真实密码的 .env 文件提交到版本控制系统
# 3. 定期更改密码以保护账户安全
# 4. 如需分享配置，请使用此 .env.example 模板文件

# ============= 使用说明 =============
# 1. 复制此文件：cp .env.example .env
# 2. 编辑 .env 文件并填写真实的用户名和密码
# 3. 根据需要调整其他配置项
# 4. 运行程序：uv run app.py 或 uv run app_cli.py